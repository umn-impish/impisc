#!/bin/bash

# scripts="configure_sd1 configure_configtxt configure_network remove_bloat install_scripts"
# for s in $scripts; do
#     echo "executing $s"
#     ./"$s"
#     echo "done $s"
# done

read -p "Reboot now? (leave blank for yes, type something for no) " conf

if [[ -z $conf ]]; then
    sudo reboot
fi
