#!/bin/bash

# Install systemd services and associated programs

# Health service
sudo cp '../processes/health_monitor' '/usr/local/bin'
sudo cp '../services/system-health.service' '/etc/systemd/system/'
sudo systemctl daemon-reload
sudo systemctl enable system-health.service
sudo systemctl start system-health.service

