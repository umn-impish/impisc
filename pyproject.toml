[build-system]
requires = ["setuptools >= 61.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ['impisc']

[project]
version = "0"
name = "impisc"
dependencies = [ ]

[project.scripts]