[build-system]
requires = ["setuptools >= 61.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ['impisc']

[project]
version = "0"
name = "impisc"
dependencies = [
 "rpi-gpio>=0.7.1",
 "spidev>=3.8",
]

[project.scripts]

[dependency-groups]
dev = [
    "ruff>=0.14.10",
]
