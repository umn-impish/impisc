# DAQBOX netplan for the Pi: assumes eth1 is the correct device

network:
    version: 2
    renderer: networkd
    ethernets:
        eth1:
            dhcp4: no
            dhcp6: no
            addresses:
                - 192.168.0.3/24

